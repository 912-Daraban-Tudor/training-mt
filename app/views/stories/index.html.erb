<p style="color: green"><%= notice %></p>

<% content_for :title, "Stories" %>

<h1>Stories</h1>

<div id="stories">
  <%= render partial: 'stories/story', collection: @stories, as: :story %>
</div>

<%= link_to "New story", new_story_path %>
